mutation UpdateProduct($input: ProductCreateInput!, $id: Int!) {
  updateProduct(input: $input, id: $id) {
    id
    image
    brand
    description
    price
    publicId
    creator {
      fullName
    }
  }
}
