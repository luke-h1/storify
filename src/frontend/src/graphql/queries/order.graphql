query Order($id: Int!) {
  order(id: $id) {
    id
    productId
    firstName
    lastName
    email
    creatorId
    createdAt
    updatedAt
    orderItems {
      id
      productTitle
      price
      qty
      orderId
      createdAt
      updatedAt
    }
  }
}
