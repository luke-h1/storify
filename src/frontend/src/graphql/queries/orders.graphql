query Orders {
  orders {
    id
    status
    total
    creatorId
    orderDetails {
      createdAt
      creatorId
      id
      orderId
      quantity
      productId
      product {
        id
        name
        image
        brand
        price
      }
    }
  }
}